---
- name: Install nginx with virtual hosts
  hosts: webservers
  remote_user: runner
  become: true
  become_method: sudo
  gather_facts: true

  vars:
    sites:
      - etis.com
      - fizfak.com
      - mehmat.ru

  roles:
    - nginx-vhosts
